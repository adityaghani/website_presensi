<?php
$file = "data.txt";
$data = file_exists($file) ? file($file, FILE_IGNORE_NEW_LINES) : [];
?>
<!DOCTYPE html>
<html lang="id">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Data Presensi</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    :root {
      --primary: #2563EB; /* Biru modern */
      --bg: #F8FAFC; 
      --card: #FFFFFF; 
      --border: #E2E8F0;
      --text: #0F172A;
    }

    body {
      background: var(--bg);
      min-height: 100vh;
      font-family: "Poppins", sans-serif;
      color: var(--text);
    }

    .container {
      max-width: 850px;
      margin-top: 60px;
      padding: 10px;
    }

    .card-table {
      background: var(--card);
      padding: 24px;
      border-radius: 12px;
      border: 1px solid var(--border);
      box-shadow: 0 4px 12px rgba(15, 23, 42, 0.08);
    }

    .btn-back {
      background: var(--primary);
      color: #fff;
      border-radius: 8px;
      text-decoration: none;
      padding: 8px 14px;
      font-weight: 500;
      transition: 0.2s ease;
    }

    .btn-back:hover {
      opacity: .9;
    }

    table {
      border-radius: 10px;
      overflow: hidden;
    }

    thead {
      background: var(--primary);
      color: #fff;
    }

    .badge {
      padding: 6px 10px;
      font-size: .85rem;
    }
  </style>
</head>
<body>

  <div class="container">
    <h2 class="text-center fw-bold mb-4">📋 Data Presensi Mahasiswa</h2>

    <div class="text-center mb-3">
      <a href="index.php" class="btn-back">← Kembali</a>
    </div>

    <?php if (isset($_GET['success'])): ?>
      <div class="alert alert-success text-center">✅ Data presensi berhasil disimpan!</div>
    <?php endif; ?>

    <div class="card-table">
      <div class="table-responsive">
        <table class="table text-center align-middle">
          <thead>
            <tr>
              <th>No</th>
              <th>Tanggal</th>
              <th>Nama</th>
              <th>NIM</th>
              <th>Keterangan</th>
            </tr>
          </thead>
          <tbody>
            <?php if (empty($data)): ?>
              <tr>
                <td colspan="5" class="py-3">Belum ada data presensi.</td>
              </tr>
            <?php else: ?>
              <?php $no = 1; foreach ($data as $baris): 
                list($tanggal, $nama, $nim, $keterangan) = explode(" | ", $baris);
              ?>
                <tr>
                  <td><?= $no++ ?></td>
                  <td><?= htmlspecialchars($tanggal) ?></td>
                  <td><?= htmlspecialchars($nama) ?></td>
                  <td><?= htmlspecialchars($nim) ?></td>
                  <td>
                    <?php if ($keterangan === "Hadir"): ?>
                      <span class="badge bg-success">Hadir</span>
                    <?php elseif ($keterangan === "Izin"): ?>
                      <span class="badge bg-warning text-dark">Izin</span>
                    <?php else: ?>
                      <span class="badge bg-danger">Alpa</span>
                    <?php endif; ?>
                  </td>
                </tr>
              <?php endforeach; ?>
            <?php endif; ?>
          </tbody>
        </table>
      </div>
    </div>

  </div>

</body>
</html>
